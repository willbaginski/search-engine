/*
 * word -- contains a function to normalize a word (convert all its letters to lowercase)
 *
 * Will Baginski, May 2020
 */

/* takes a char * as input
 * turns every uppercase letter lowercase
 * all other characters are unaffected
 */
void NormalizeWord(char *word);
